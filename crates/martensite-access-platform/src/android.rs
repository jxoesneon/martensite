//! Android accessibility adapter — the JNI FFI boundary (stub).
//!
//! This module is intentionally empty: the `accesskit_android`
//! `InjectingAdapter` + `embedded-dex` JNI glue for
//! `aarch64-linux-android` is contributed by the parallel Android
//! workstream. It exists so the crate's module layout is stable while
//! that work lands.
