//! Industrial Workstation — Martensite's flagship dogfooding example.
//!
//! Thin shell over the library crate — see `src/lib.rs` for the
//! module docs and `run_cli` for the windowed/headless entry.

fn main() {
    industrial_dashboard::run_cli();
}
